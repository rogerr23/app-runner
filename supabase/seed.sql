-- The baseline seed is intentionally empty.
--
-- Test users are created through Supabase Auth instead of inserting directly
-- into auth.users. This keeps the seed compatible with future Auth schema
-- changes. See docs/local-development.md for the demo-account workflow.

